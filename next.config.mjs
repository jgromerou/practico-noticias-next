/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
        domains: ["via.placeholder.com"], // agrega aquí todos los dominios de imágenes externas
    },
};

export default nextConfig;
